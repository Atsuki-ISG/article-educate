# 教育のしごとOS

## 思想
「データを入れるほど賢くなる、教育発信と業務の基盤」を作る。

- **複利構造**: データ蓄積→ネタ発掘→原稿執筆→フィードバック→ナレッジ化のループが回るたびに出力の質が上がる
- **人間はジャッジだけ**: AIが叩き台・分析・提案を全部やる。人間は「OK/NG」の判断と最終確認だけ
- **痛みから育てる**: 最初から完璧を目指さない。使って不便を感じたら改善する
- **拡張前提**: 記事執筆から始めるが、業務全般に広げられる構造にしておく

## 基本情報
- 発信者: 校長、教育委員会の指導主事など一定の立場のある人
- 対象読者: 地域の住民（保護者、地域の方々）
- 主な発信: 教育広報記事（保護者向け啓発コンテンツ）

## 回答スタイル
- 結論ファースト。前置き・段階報告は最小限
- 指摘すべきことは率直に指摘
- 記事の執筆時は `.claude/rules/` の該当ルールに従う

## 構成

### リポジトリ内（git管理）
| ディレクトリ | 役割 |
|---|---|
| `.claude/agents/` | 6エージェント。Claude Code本体がOrchestrator |
| `.claude/rules/` | 執筆ルール・業務ルール（詳細はここに集約） |
| `.claude/plans/` | 実装プラン。意図（なぜ必要か）と選択理由を含める |
| `articles/` | 完成記事（公開用） |
| `articles-draft/` | 下書き・作成中の原稿 |
| `articles-example/` | 参考記事（外部の良記事など） |
| `data/source/` | 過去の著作物・業務データ（下記参照） |
| `data/topics/` | 発掘したネタ・トークテーマ |
| `data/patterns/` | 蓄積された執筆パターン・反応パターン |
| `knowledge/` | 参考文献・引用元の整理 |

### data/source/ の構成（git管理外・2,101件）

| ディレクトリ | 件数 | 内容 |
|---|---|---|
| `だより/` | 754 | 心姿創全(R2-R5)、行動創全(R6-R8)、Your smile、学校だより |
| `朝会素材/` | 580 | 言葉の力(300+)、言葉クイズ(140+)、しょうもない話、勇気づけの詩 |
| `メッセージ/` | 349 | 一言メッセージ（保護者・教職員向け短文） |
| `あいさつ/` | 165 | 式典スピーチ原稿（校長時代・課長時代・所長時代） |
| `投稿/` | 136 | 勇気づけの教育（新聞連載コラム60+回）、新聞投稿 |
| `資料/` | 55 | キャリア教育、校長資料、参考文献（PDF） |
| `講話/` | 52 | 校長講話、研修スライド |
| `詩/` | 10 | オリジナルの詩 |

詳細は `data/source/inventory.md` を参照。

### リポジトリ外（Claude Code自動管理・git対象外）
| ディレクトリ | 役割 |
|---|---|
| `~/.claude/projects/.../memory/` | 会話を跨ぐ永続メモリ。MEMORY.mdがインデックス |

## エージェント体系

Claude Code本体がOrchestratorとして6エージェントを使い分ける。

| エージェント | 役割 | 状態 |
|---|---|---|
| **writer** | 執筆ルールに従って記事の下書きを書く | 運用中 |
| **editor** | 記事をルール・チェックリストに照らしてレビュー | 運用中 |
| **fact-checker** | 引用・数値・法令の正確性を検証、裏取り | 運用中 |
| **researcher** | 業務データから記事ネタ・トークテーマを発掘 | スタブ |
| **analyst** | 業務データの要約・整理・分析（業務全般） | スタブ |
| **knowledge-builder** | FBからパターン抽出、ルール・ナレッジ更新 | スタブ |

**記事作成の基本フロー:** writer → editor → fact-checker → 修正 → `articles/` に移動

## ルール体系

詳細ルールは `.claude/rules/` に配置。CLAUDE.mdには書かない。

| ファイル | 内容 |
|---|---|
| `writing-style.md` | 文体・トーン・専門用語の扱い |
| `structure.md` | 記事の構成・階層・まとめの作り方 |
| `expression.md` | 表現の指針・読者との距離感・強調手法 |
| `education-specific.md` | 公文書との使い分け・引用ルール・プライバシー配慮 |
| *(今後追加)* | 業務関連ルールもここに足していく |

## ナレッジ蓄積の方針

- 記事執筆後 → フィードバックからパターン抽出 → `data/patterns/` に追記
- 業務データ投入後 → 記事ネタ・トークテーマを発掘 → `data/topics/` に追記
- 参考記事分析後 → 使える構成・表現パターン → `data/patterns/` に追記
- 蓄積したパターン → 次の執筆に還元 → 精度UP → ループ

## 5原則

### 1. Less is more — 短く保て
- ファイルは必要最小限の情報量にする
- CLAUDE.mdに詳細ルールを書かない。rulesに逃がす

### 2. Progressive Disclosure — 必要な時に必要な情報を
- 執筆ルールの詳細は `.claude/rules/` に配置
- CLAUDE.mdは「何がどこにあるか」だけ示す

### 3. WHAT/WHY/HOWで書け
- ルールには「なぜそうするか」の理由を含める
- 良い例・悪い例を必ずセットにする

### 4. 検証手段を書け
- 記事の完了チェックリストは `rules/` 内に配置

### 5. 痛みから育てろ
- 問題が起きたら → ルール追加・パターン追記
- フィードバック → 抽象化 → ルール反映のループを回す
